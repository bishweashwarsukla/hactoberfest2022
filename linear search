#include<iostream>
Using namespace std;
Bool search(int arr[]; int size; int key)
{
    For(int I = 0; i<size; i++)
{
    If (arr[i] == key)
{
       Return 1;
}
}
return 0;
}
Int main(){
     Int arr[i] = {5, 7, -2, 10, 22, -2, 0, 5, 22, 1};
Cout<<”Enter the element to search for  << endl;
Int key;
Cin>>key;
Bool found = search(arr,10,key);
If(found)
{
      Cout<<”key is present”<<endl;
}
Else
{
Cout<<”
    Return 0;
}
